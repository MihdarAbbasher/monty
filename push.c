#include "monty.h"

/**
 * push_int - push to stack
 * @n: int to be pushed
 * Rerurn: no thing
 */

void push_int(stack_t *stack, char *n)
{
	printf("push %s\n", n);
	if(!stack)
	{
		printf("0000");
	}

}
